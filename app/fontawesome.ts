import { config } from "@fortawesome/fontawesome-svg-core";
import "@fortawesome/fontawesome-svg-core/styles.css"; // FA base styles
config.autoAddCss = false; // prevent FA from adding its CSS again at runtime